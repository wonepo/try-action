name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    name: Build and Deploy on ubuntu latest
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      #           - name: Set up JDK 22.0
      #             uses: actions/setup-java@v4
      #             with:
      #               distribution: microsoft
      #               java-version: 21
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
      #      - name: unit Test
      #        run: Unit Test --file pom.xml

      #      - name: Build the application
      #        run: |
      #          mvn clean
      #          mvn - B package --file pom.xml

      - name: Build Docker Image
        uses: actions/build-push-action@v2
        with:
          context: .
          dockerfile: Dockerfile
          push: false
          tags: ${{secrets.DOCKER_USERNAME}}/${{github.run_attempt}}

      - name: Loging to Docker Hub
        uses: actions/login-action@v1
        with:
          username: ${{secrets.DOCKER_USERNALE}}
          password: ${{secrets.DOCKER_ACCESS_TOKEN}}

      - name: Push to Docker Hub
        uses: actions/build-push-action@v2
        with:
          context: .
          dockerfile: Dockerfile
          push: true
          tags: ${{secrets.DOCKER_USERNAME}}/${{github.run_attempt}}
